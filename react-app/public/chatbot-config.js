// Configure Chatbot without rebuilding the app
// Set webhookUrl to your backend endpoint to receive chat contact messages (optional)
// Example: window.CHATBOT_CONFIG = { webhookUrl: 'https://your-endpoint.example.com/api/contact' }
window.CHATBOT_CONFIG = window.CHATBOT_CONFIG || {
  webhookUrl: ''
}
